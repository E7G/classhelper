- 新增真正的“课程”层：课程不再等于当前 PDF。学习通课程和本地自定义课程都可以独立打开，即使没有任何 PDF 也能开始听课、记笔记和进行课堂问答。
- App 首页改为“我的课程”：可以选择课程、直接进入课程、新建本地课程、进入学习通同步，以及查看课程内资料。
- 本地课程支持 0～N 份 PDF：先创建课程，再按需加入多份教材/讲义；课程知识库会在 SQLite 内聚合这些 PDF 的索引，不把整套资料加载进 Java Heap。
- 当前课程与当前文档完全解耦：currentCourseId/currentCourseKnowledgeDocumentId 保存课程上下文；currentDocumentId 只代表当前正在阅读的具体 PDF。
- 课堂问题检索优先使用当前课程知识库；如果同时打开了某个 PDF，则该 PDF 的当前页/附近页作为更强的局部上下文参与回答。
- 学习通同步后会把课程登记为独立课程容器，不再只是生成一个“学习通知识文档”。
- 学习通附件如果提供可读取的 PDF 版本，会持久缓存到 App 私有 course-resources 目录，并登记成课程资源；同步结束后不会再删除，可从“我的课程”里反复用内置 PDF 阅读器打开。
- 学习通非 PDF/无法缓存的资源仍保留资源名称和在线地址，不伪装成可打开的 PDF。
- 退出学习通只清除账号密码/Cookie，不删除已经同步到本地的课程和 PDF 资源。
- 保留 1.10.9 的“思考停顿门”：partial 不处理问题；final 疑似问题需在 endpoint 后继续静默约 1.2 秒，老师继续讲话则取消问题，不调用 LLM。
- 保留高质量 Zipformer 配置：4-path contextual beam、课程/PDF 热词、自适应 60/120/240ms 喂流、无主动丢帧 PCM 环形缓冲、ASR/业务异步隔离，以及仅恢复 AudioRecord 的非破坏 watchdog。
- 保留低内存 PDF/OCR 和问题/答案不发送额外系统通知。
