- 修复 1.9.1 为防 OOM 而丢弃旧音频帧可能造成的漏句问题：1.9.2 不再主动丢弃课堂音频。
- Zipformer 输入改为固定大小 PCM16 环形缓冲，不再为每个约 60 ms 录音帧创建 FloatArray 和 Runnable；内存占用固定，不会随上课时长持续增长。
- 环形缓冲按原始 16-bit PCM 保存，约 180 秒缓冲只占约 5.5 MiB，可吸收设备短时卡顿/降频造成的识别积压。
- 改为每 4 个录音帧（约 240 ms）合并后送入 OnlineRecognizer，显著减少 JNI/解码调用次数，让 Zipformer 更容易追上实时音频。
- AudioRecord 后备缓冲扩大到约 4 秒，为系统调度抖动和短时 CPU 峰值提供额外保护，降低采集端丢音风险。
- 停止听课时会继续处理环形缓冲内最后不足一个批次的音频，避免最后半句话被截掉。
- 保留 Zipformer Streaming Transducer、实时 partial、endpoint 自动断句、PDF/手动热词、课堂记录、自动笔记和左下角字幕。
