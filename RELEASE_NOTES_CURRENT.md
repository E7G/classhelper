- 修复课堂识别过程中“识别到问题后突然不继续识别 / 看起来卡住”的线程耦合问题：Zipformer ASR 回调现在只做快速事件投递，不再直接执行数据库、问题检测、LLM、自动笔记或 PDF 匹配。
- 新增独立 ASR 事件队列：最终转写按顺序异步写入数据库并更新问题检测状态，任何业务处理变慢都不会阻塞 Zipformer 解码线程。
- 问题识别与回答改为独立串行后台线程；课程资料检索、最近课堂上下文读取和 LLM 流式回答均与语音识别完全分离。
- 自动笔记改为独立低优先级后台线程，与问题回答也彼此分开；笔记生成卡顿不会占住识别或问题回答线程。
- PDF 页面匹配继续异步执行；字幕/UI/通知更新也从 ASR 回调线程异步投递，识别线程优先持续收音和解码。
- 业务层异常只记录日志，不再把正在正常工作的 ASR 误标为“停止/错误”；只有真正的录音或识别错误才更新 ASR 状态。
- 保留 1.9.2 的无丢帧 PCM16 环形缓冲、240 ms 批量解码、endpoint 自动断句、实时 partial、PDF/手动热词和停止时尾句收尾逻辑。
