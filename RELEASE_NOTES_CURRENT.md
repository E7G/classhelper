- 回滚 1.11.x 的“课程容器 / 我的课程”架构，恢复以当前 PDF 为课堂主上下文的原有使用方式。
- ReaderActivity 重新作为应用首页/启动入口；不再使用 CourseHubActivity，也不再维护 currentCourseId/currentCourseKnowledgeDocumentId 等课程级状态。
- 学习通恢复为课程资源同步来源：同步后的章节、课件文本继续作为课堂知识补充，但不会把学习通课程提升为独立的应用级课程容器。
- 本地自定义课程、多 PDF 聚合课程、course-resources 持久课程资源层已撤回；课堂仍围绕当前打开的 PDF 工作。
- 保留学习通账号密码加密保存、真实课程名称、章节/附件/PDF 文本同步、低内存分批索引。
- 保留 1.10.9 的“思考停顿门”：partial 不做问题检测，final 疑似问题需继续静默约 1.2 秒；老师继续讲话则取消，不调用 LLM。
- 保留高质量 Zipformer：4-path contextual beam、PDF/学习通上下文热词、自适应 60/120/240ms 喂流、无主动丢帧 PCM 环形缓冲、ASR 与业务异步隔离。
- 保留 AudioRecord 非破坏自动恢复、低内存 PDF/OCR，以及问题/答案不发送额外系统通知。
