- 新增学习通/超星课程资源接入：设置页可直接用账号 + 密码登录当前学习通账号，不再要求手动 Cookie 或扫码。
- 登录兼容现有开源实现常见的两套 passport2/fanyalogin 参数：旧版 DES 密码请求与新版 AES-CBC 账号/密码请求自动回退。
- 账号保存在普通设置中；密码和登录 Cookie 均通过应用现有 AndroidKeyStore + AES-GCM SecretStore 加密保存。Cookie 失效时会自动使用保存的账号密码重新登录，无需反复输入。
- 新增课程选择与同步：读取当前账号可见课程、章节、knowledge/cards 资源描述，并解析当前账号本来有权限访问的课程资料。
- 章节正文、资源说明和可读取 PDF 课件会转换为本地知识库片段；PDF 下载流式写临时文件，不把整个课件塞进 Java 堆，完成索引后删除临时文件。
- 视频/音频等媒体不整包下载到内存，只保存课程资源元数据与授权资源地址，供课程上下文使用。
- 课堂问题检索新增“当前学习通课程”优先层：当前阅读 PDF 仍为第一优先级，随后检索已绑定学习通课程，再回退整个资料库。
- 学习通资源仅做只读访问：不刷任务、不提交学习进度、不自动答题/签到，也不绕过课程权限。
- 保留 1.9.3 的 ASR/问题回答/自动笔记异步隔离、1.9.2 无丢帧 PCM16 环形缓冲和 Zipformer Streaming 实时识别。
