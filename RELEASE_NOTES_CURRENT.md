- 保持 PDF-first 架构：当前打开的 PDF 仍然是课堂主上下文，不恢复“课程容器 / 我的课程”层。
- 学习通“课程资料”改为课程级平铺列表：进入课程后自动扫描全部章节并汇总所有当前账号可访问的附件，不再提供章节下拉筛选。
- 资料扫描按课程章节顺序逐章读取，并在顶部显示汇总进度；最终列表不按章节分组，只在每个资料的次要信息中标注来源章节。
- 对相同 objectId 或相同在线资源进行跨章节去重，避免同一课件因为章节/card 结构重复展示。
- 资料卡片和“预览”按钮都可以直接打开资料；超星附件存在 PDF 转换版时，会按需下载到 App 私有 chaoxing-view 缓存，并通过 FileProvider 交给现有 ReaderActivity 预览。
- PDF 缓存会复用，重复预览同一课件不需要每次重新下载；单个查看缓存限制 160 MB，下载使用流式写盘，不把整份文件放入 Java Heap。
- 非 PDF 资料或仅提供在线地址的资源交给系统浏览器/对应应用打开；应用内不嵌入网页运行时，遵守项目安全校验规则。
- 资料查看链路只读，不提交学习进度、不完成任务、不签到。
- 保留学习通账号密码加密保存、真实课程名称、课程知识库同步和低内存分批索引。
- 保留 1.10.9 的思考停顿门、高质量 Zipformer、4-path contextual beam、自适应 60/120/240ms 喂流、无主动丢帧 PCM 环形缓冲和 ASR/业务异步隔离。
- 保留 AudioRecord 非破坏自动恢复、低内存 PDF/OCR，以及问题/答案不发送额外系统通知。
